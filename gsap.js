

    gsap.from('.first-section', { duration: 1, y: '-100%', ease: 'sine',  });

    gsap.from('.logo', { duration: 1, opacity: 0, delay: 1 });
    gsap.from('.links', { duration: 1, opacity: 0, delay: 1 });
    gsap.from('.text-image', { duration: 1, opacity: 0, delay: 2 });
    gsap.from('.title', { duration: 1, opacity: 0, delay: 3 });
    gsap.from('.cards', { duration: 1, opacity: 0, delay: 3 });
    gsap.from('.third-section', { duration: 1, opacity: 0, delay: 3 });
    gsap.from('.fourth-section', { duration: 1, opacity: 0, delay: 3 });
    gsap.from('.footer', { duration: 1, opacity: 0, delay: 3 });

